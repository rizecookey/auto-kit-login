{
  "dependencies": {
    "browserify": "^17.0.0"
  },
  "name": "auto-kit-login",
  "version": "1.0.0-dev",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && node build.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rizecookey/auto-kit-login.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Automatic Shibboleth login on KIT pages.",
  "devDependencies": {
    "fs-extra": "^11.2.0",
    "webextension-polyfill": "^0.10.0"
  }
}
